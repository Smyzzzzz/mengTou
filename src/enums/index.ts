// 头像形状
export enum WrapperShape {
  Circle = 'circle',
  Square = 'square',
  Squircle = 'squircle'
}
// 语言
export const enum Locale {
  ZH = 'zh',
  EN = 'en'
}

// 小零件

export enum WidgetType {
  Face = 'face'
}

// 脸型

export enum FaceShape {
  Base = 'base'
}
